<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common.address.service.mapper.AddressApiMapper">

	<select id="getCityList" resultType="map">
	/* AddressApiMapper.getCityList */
		/* SELECT SIDO "sido"
		  FROM COMUSER.TBD_COM_INF_ZIPCOD 
	     GROUP BY SIDO */
	   SELECT 1 "sido" FROM DUAL
	</select>

</mapper>